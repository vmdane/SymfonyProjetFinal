<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Biblio Communautaire{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans min-h-screen flex flex-col">

    <!-- HEADER -->
    <header class="bg-blue-900 px-6 py-4 shadow-md">
        <nav>
            <ul class="flex gap-6 flex-wrap">
                <li>
                    <a href="{{ path('app_home') }}"
                       class="text-gray-100 font-semibold px-3 py-2 rounded-md hover:bg-blue-500 hover:text-white transition">
                        Accueil
                    </a>
                </li>
                <li>
                    <a href="{{ path('app_book_index') }}"
                       class="text-gray-100 font-semibold px-3 py-2 rounded-md hover:bg-blue-500 hover:text-white transition">
                        Books
                    </a>
                </li>

                {# Liens visibles uniquement si connecté #}
                {% if app.user %}
                    <li>
                        <a href="{{ path('ma_bibliotheque') }}"
                           class="text-gray-100 font-semibold px-3 py-2 rounded-md hover:bg-blue-500 hover:text-white transition">
                            Ma Bibliothèque
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_favoris') }}"
                           class="text-gray-100 font-semibold px-3 py-2 rounded-md hover:bg-blue-500 hover:text-white transition">
                            favoris
                        </a>
                    </li>
                {% endif %}

                {# Connexion / Déconnexion #}
                {% if app.user %}
                    <li>
                        <a href="{{ path('app_logout') }}"
                           class="text-gray-100 font-semibold px-3 py-2 rounded-md hover:bg-blue-500 hover:text-white transition">
                            Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('app_login') }}"
                           class="text-gray-100 font-semibold px-3 py-2 rounded-md hover:bg-blue-500 hover:text-white transition">
                            Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </nav>
    </header>

    <!-- MAIN -->
    <main class="flex-grow max-w-4xl w-full mx-auto my-10 p-8 bg-white rounded-xl shadow-lg">
        {% block body %}
        {# Le contenu principal des pages ira ici #}
        {% endblock %}
    </main>

    <!-- FOOTER -->
    <footer class="bg-blue-900 text-gray-300 text-center text-sm py-4 mt-auto">
        <p>© {{ "now"|date("Y") }} - Mon site Symfony</p>
    </footer>

</body>
</html>
