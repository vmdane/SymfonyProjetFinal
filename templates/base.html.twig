<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Biblio Communautaire{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans min-h-screen flex flex-col">

    <!-- HEADER -->
    <header class="bg-blue-900 shadow-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center py-4">
                <!-- Logo / Accueil -->
                <div class="flex-shrink-0">
                    <a href="{{ path('app_home') }}" class="text-white text-2xl font-bold">📚 Biblio</a>
                </div>

                <!-- Menu principal -->
                <nav class="hidden md:flex space-x-6 items-center">
                    <a href="{{ path('app_home') }}" class="text-gray-100 hover:text-white font-medium transition">Accueil</a>
                    <a href="{{ path('app_book_index') }}" class="text-gray-100 hover:text-white font-medium transition">Catalogue</a>

                    {% if app.user %}
                        <a href="{{ path('app_user_profile', {'id': app.user.id}) }}" class="text-gray-100 hover:text-white font-medium transition">Mon profil</a>
                        <a href="{{ path('ma_bibliotheque') }}" class="text-gray-100 hover:text-white font-medium transition">Mes bibliothèques</a>
                        <a href="{{ path('app_favorite') }}" class="text-gray-100 hover:text-white font-medium transition">Favoris</a>
                        <a href="{{ path('app_loan_index') }}" class="text-gray-100 hover:text-white font-medium transition">Prêts</a>
                    {% endif %}

                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('admin_dashboard') }}" class="text-yellow-300 hover:text-white font-medium transition">Admin</a>
                    {% endif %}
                </nav>

                <!-- Auth / Actions -->
                <div class="flex items-center space-x-4">
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}" class="text-red-300 hover:text-white font-medium transition">Déconnexion</a>
                    {% else %}
                        <a href="{{ path('app_login') }}" class="text-gray-100 hover:text-white font-medium transition">Connexion</a>
                        <a href="{{ path('app_register') }}" class="text-white bg-blue-700 px-3 py-1 rounded hover:bg-blue-600 transition text-sm">Inscription</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </header>


    <!-- MAIN -->
    <main class="flex-grow w-full mx-auto bg-white rounded-xl shadow-lg">
        {% block body %}
        {# Le contenu principal des pages ira ici #}
        {% endblock %}
    </main>

    <!-- FOOTER -->
    <footer class="bg-blue-900 text-gray-300 text-center text-sm py-4 mt-auto">
        <p>© {{ "now"|date("Y") }} - Mon site Symfony</p>
    </footer>

</body>
</html>
