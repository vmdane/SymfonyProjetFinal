{# templates/author/_form.html.twig #}
{{ form_start(form) }}

    {{ form_row(form.name) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.birthdate) }}
    {{ form_row(form.biography) }}

    {# Ajoute ici la sélection des books #}
    {{ form_row(form.books) }}

    <button class="btn">{{ button_label|default('Enregistrer') }}</button>

{{ form_end(form) }}
